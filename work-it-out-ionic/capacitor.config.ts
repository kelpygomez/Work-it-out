import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'work-it-out-ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
